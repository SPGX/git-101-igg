let temp = ['apple', 'banana', 'Kiwi']
temp.forEach((element, idx) => {
    console.log(idx, element)
});

//let newValue = temp.map(element => console.log(element, idx) => {
//    return `${idx+1}.${element}`
//})
//console.log(newValue)*//